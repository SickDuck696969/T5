# Simple JFrame Example

This workspace contains a minimal Java Swing application that opens a `JFrame` window.

## Files

- `src/Main.java` - application entry point
- `src/MainFrame.java` - defines a basic JFrame with a label and button

## Run

From the workspace root:

```sh
javac -d out src/*.java
java -cp out Main
```
